services:
  - type: web
    name: hono-api
    runtime: bun
    buildCommand: bun install && cd apps/hono-api && bun run build
    startCommand: cd apps/hono-api && bun run start
    envVars:
      - key: NODE_ENV
        value: production

